# Environment Variables for HRA Legal Website
# Copy this content to .env.local and fill in your actual values

# Sanity CMS Configuration
NEXT_PUBLIC_SANITY_PROJECT_ID=mvclci23
NEXT_PUBLIC_SANITY_DATASET=production

# Instructions:
# 1. Create a new file named '.env.local' in the project root
# 2. Copy the above variables into that file
# 3. Replace the values with your actual Sanity project credentials
# 4. The .env.local file will be automatically ignored by git for security 